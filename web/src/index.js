import React from 'react';
// O ReactDOM varia de acordo com o ambiente que estamos,
// no browser é o DOM, no mobile é o react native, na tv reactTV ...
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
// JSX (Javascript + XML)
